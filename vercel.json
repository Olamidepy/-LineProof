{
  "buildCommand": "pnpm install --no-frozen-lockfile && pnpm --filter @lineproof/sdk build && pnpm --filter @lineproof/frontend build",
  "outputDirectory": "frontend/dist",
  "installCommand": "npm install -g pnpm@9",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
